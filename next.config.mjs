/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	swcMinify: true,
	// If you're not using API routes or server-side features:
	// output: 'export',
};

export default nextConfig;
